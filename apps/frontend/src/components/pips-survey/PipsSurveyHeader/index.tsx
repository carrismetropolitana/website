'use client';
/* * */

interface PipsSurveyHeaderProps {
	pipId: string
}

/* * */

export function PipsSurveyHeader({ pipId }: PipsSurveyHeaderProps) {
	//

	//
	// A. Setup variables

	//
	// B. Render components

	return (
		<p>PIP {pipId}</p>
	);

	//
}
