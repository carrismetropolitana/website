/* * */

export interface CampaignData {
	body: string
	featured_image?: {
		filename: string
		thumbnailURL: string
		url: string
	}
	id: string
	is_unlisted?: boolean
	publishedAt: string
	slug: string
	status: string
	title: string
	updatedAt: string
}

/* * */
