/* * */

import type { Field } from 'payload';

/* * */

export const hasDefaultSurfaceField: Field = {
	admin: {
		position: 'sidebar',
	},
	label: 'Default Surface?',
	name: 'has_default_surface',
	type: 'checkbox',
};
